{
  "name": "auxeira-backend-serverless",
  "version": "1.0.0",
  "description": "SSE Platform Serverless Backend with Solana Integration",
  "scripts": {
    "build": "sam build",
    "deploy": "sam deploy --guided",
    "deploy-prod": "sam deploy",
    "local": "sam local start-api",
    "test": "npm run test --prefix aws-lambda/functions"
  },
  "dependencies": {
    "@solana/web3.js": "^1.87.0",
    "@noble/ed25519": "^2.0.0",
    "@noble/hashes": "^1.3.0"
  },
  "devDependencies": {
    "aws-sam-cli": "^1.0.0"
  }
}
