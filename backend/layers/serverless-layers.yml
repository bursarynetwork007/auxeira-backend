service: auxeira-layers

provider:
  name: aws
  runtime: nodejs18.x
  region: us-east-1

layers:
  heavyDeps:
    path: heavy-deps
    name: auxeira-heavy-dependencies
    description: "Heavy dependencies for Auxeira"
    compatibleRuntimes:
      - nodejs18.x
    retain: false
